import * as React from 'react';
import App from './App';
import {
    NativeModules,
} from 'react-native';

class AppRouter extends React.Component < {}, {} > {

    render() {
        return <App/>
    }
}

export default AppRouter;